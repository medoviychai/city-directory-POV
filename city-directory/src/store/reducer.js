const initialState = {
   cities: '',
   countOfCities: '',
}

function reducer(state = initialState, action) {
   if (action.type === '') {

   }
   return initialState;
}
// console.log("reducer", initialState)
export default reducer;